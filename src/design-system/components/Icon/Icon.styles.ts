/**
 * Icon Styles
 * Define estilos e funções auxiliares para o componente Icon
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  pressable: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  disabled: {
    opacity: 0.5,
  },
});
