import { Layout } from '@components/Layout/Layout';
import { ScrollView, Text } from 'react-native';

function Home() {
  return (
    <Layout>
      <ScrollView>
        <Text>Home</Text>
      </ScrollView>
    </Layout>
  );
}

export default Home;
